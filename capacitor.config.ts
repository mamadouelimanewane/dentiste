import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dentoprestige.elite',
  appName: 'DentoPrestige Elite',
  webDir: 'out'
};

export default config;
